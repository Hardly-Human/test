<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1>Welcome to the Image Sharing Platform!</h1>

  <% if (user) { %>
    <p>Hello, <%= user.name %>!</p>
    <a href="/profile">View Profile</a> |
    <a href="/logout">Logout</a>
  <% } else { %>
    <a href="/signup">Sign Up</a>  <!-- Signup Button -->
    <a href="/login">Login</a>  <!-- Login Button -->
  <% } %>

</body>
</html>
